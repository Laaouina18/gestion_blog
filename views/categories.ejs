<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Catégories</title>
</head>
<body>
    <h1>Catégories</h1>
    <ul>
        <% categories.forEach(category => { %>
            <li>
                <a href="/categories/<%= category.IdCategorie %>"><%= category.Titre %></a>
                <p><%= category.Description %></p>
            </li>
        <% }); %>
    </ul>
</body>
</html>
